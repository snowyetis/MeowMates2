  <nav class="navbar navbar-default navbar-fixed-top am-top-header">
    <div class="container-fluid">
      <div class="navbar-header">
        <%= link_to "MeowMates", root_url, class: "navbar-brand" %>
      </div>
      <% if user_signed_in? %>
        <a href="#" class="am-toggle-right-sidebar">
          <span class="icon s7-menu2"></span>
        </a>
      <% end %>
      <a href="#" data-toggle="collapse" data-target="#am-navbar-collapse" class="am-toggle-top-header-menu collapsed">
        <span class="icon s7-angle-down"></span>
      </a>
      <div id="am-navbar-collapse" class="collapse navbar-collapse">
          <ul class="nav navbar-nav am-nav-right">
            <li><%= link_to 'Home', home_path %></li>
            <li><%= link_to 'About', about_path %></li>
            <% if user_signed_in? %>
              <li class="dropdown"><a href="#" data-toggle="dropdown" role="button" aria-expanded="false" class="dropdown-toggle">
                  User Options<span class="angle-down s7-angle-down"></span></a>
                  <ul role="menu" class="dropdown-menu">
                    <%= render 'shared/links' %>
                  </ul>
              </li>
            <% end %>
            <li class="dropdown"><a href="#" data-toggle="dropdown" role="button" aria-expanded="false" class="dropdown-toggle">
              <% if user_signed_in? %> <%= current_user.name %> <span class="angle-down s7-angle-down"></span></a> <% else %> Log In or Register <span class="angle-down s7-angle-down"></span></a> <% end %>
              <ul role="menu" class="dropdown-menu">
                  <% if user_signed_in? %>
                    <li><%= link_to('Your Profile', user_path(current_user)) %></li>
                    <li><%= link_to('Sign out', destroy_user_session_path, method: :delete) %></li>
                  <% else %>
                    <li><%= link_to 'Sign up', new_user_registration_path %></li>
                    <li><%= link_to 'Sign in', user_session_path %></li>
                  <% end %>
                </ul>
            </li>
          </ul>
          <% if user_signed_in? %>
            <ul class="nav navbar-nav navbar-right am-icons-nav">
              <li><%= render 'shared/user_notification_bar' %></li>
            </ul>
          <% end %>
      </div>
    </nav>
    <% if user_signed_in? %>
      <%= render 'shared/sidebar' %>
    <% end %>
